<%= form_with(model: test_pundit) do |form| %>
  <% if test_pundit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(test_pundit.errors.count, "error") %> prohibited this test_pundit from being saved:</h2>

      <ul>
        <% test_pundit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
